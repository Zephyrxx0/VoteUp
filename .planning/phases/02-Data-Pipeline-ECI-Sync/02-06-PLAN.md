---
phase: 02-Data-Pipeline-ECI-Sync
plan: 06
type: execute
wave: 3
depends_on: ["03"]
files_modified: [apps/frontend/src/app/admin/dashboard/page.tsx, apps/backend/src/routes/admin.ts]
autonomous: true
requirements: [ADM-01]

must_haves:
  truths:
    - "Only users with 'admin' custom claim can access the dashboard"
    - "Admins can override election stages and specific AC dates"
    - "All admin actions are recorded in an Audit Log"
  artifacts:
    - path: "apps/frontend/src/app/admin/dashboard/page.tsx"
      provides: "Admin Data Editor UI"
    - path: "apps/backend/src/routes/admin.ts"
      provides: "Secure Admin SDK routes"
---

<objective>
Create a secure Admin Dashboard to allow manual overrides of election data and stages. This is critical for handling sync errors or schedule changes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-Data-Pipeline-ECI-Sync/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Admin Custom Claims & Route Protection</name>
  <files>apps/backend/src/routes/admin.ts, apps/backend/src/middleware/auth.ts</files>
  <action>
    - Implement `setAdminClaim(uid)` helper using Firebase Admin SDK.
    - Create `isAdmin` middleware verifying `admin: true` claim in ID tokens.
    - Apply middleware to all `/api/admin/*` routes in `src/routes/admin.ts`.
  </action>
  <verify>
    <automated>pnpm jest apps/backend/tests/admin-auth.test.ts</automated>
  </verify>
  <done>Non-admin users receive 403 Forbidden on admin routes.</done>
</task>

<task type="auto">
  <name>Task 2: Build Admin Dashboard UI</name>
  <files>apps/frontend/src/app/admin/dashboard/page.tsx</files>
  <action>
    - Create search/filter view for constituencies.
    - Implement "Edit Stage" dialog with dropdown for the 8 stages.
    - Add fields for manual date overrides.
    - Use optimistic UI updates.
  </action>
  <verify>
    <automated>pnpm playwright test apps/frontend/tests/admin/dashboard.spec.ts</automated>
  </verify>
  <done>Admins can successfully update AC stages via the UI.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Admin Audit Logging</name>
  <files>apps/backend/src/services/db/audit-log.ts</files>
  <action>
    - Create `audit_logs` collection in Firestore.
    - Record `{ admin_id, action, target_id, old_value, new_value, timestamp }`.
    - Trigger log creation on successful `/api/admin/*` writes.
  </action>
  <verify>
    <automated>pnpm jest apps/backend/src/services/db/audit-log.test.ts</automated>
  </verify>
  <done>All admin edits are traceable via the Audit Log collection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin -> Admin API | Privileged access boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Elevation of Privilege | Admin Claims | mitigate | Only allow setting admin claims via secure CLI script or super-admin. |  
| T-02-06-02 | Information Disclosure | Admin UI | mitigate | Ensure sensitive AC metadata is only sent to Admin UI. |
</threat_model>

<success_criteria>
- Dashboard loads and authenticates admin in < 2s.
- Audit logs capture 100% of state-changing operations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-Data-Pipeline-ECI-Sync/02-06-SUMMARY.md`
</output>
