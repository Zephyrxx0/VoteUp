---
phase: 02-Data-Pipeline-ECI-Sync
plan: 05
type: execute
wave: 2
depends_on: ["01"]
files_modified: [apps/frontend/src/components/discovery/gps-detect.tsx, apps/frontend/src/components/discovery/epic-search.tsx, apps/frontend/src/app/discovery/page.tsx]
autonomous: true
requirements: [MAP-01, VAL-01]

must_haves:
  truths:
    - "User can trigger GPS discovery and see their mapped constituency"
    - "EPIC search provides real-time validation feedback"
    - "Manual drill-down allows selection if GPS/EPIC fail"
  artifacts:
    - path: "apps/frontend/src/components/discovery/gps-detect.tsx"
      provides: "GPS location capture & mapping"
    - path: "apps/frontend/src/components/discovery/epic-search.tsx"
      provides: "Voter ID validation UI"
---

<objective>
Update the frontend discovery flow to prioritize GPS-based constituency identification while supporting EPIC search and manual drill-down as fallbacks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-Data-Pipeline-ECI-Sync/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement GPS Auto-detect Flow</name>
  <files>apps/frontend/src/components/discovery/gps-detect.tsx</files>
  <action>
    - Use browser Geolocation API to get coordinates.
    - Call backend `/api/geo/map`.
    - Display constituency card with "Is this your constituency?" confirmation.
    - Handle permission denied/unavailable states.
  </action>
  <verify>
    <automated>pnpm playwright test apps/frontend/tests/discovery/gps.spec.ts</automated>
  </verify>
  <done>User location is successfully converted to a constituency name in UI.</done>
</task>

<task type="auto">
  <name>Task 2: Implement EPIC Search UI Component</name>
  <files>apps/frontend/src/components/discovery/epic-search.tsx</files>
  <action>
    - Create input field for EPIC number.
    - Implement real-time validation using `src/lib/validation.ts` (created in 02-01).
    - Show success/warning/error states based on validation output.
    - Button to "Search & Verify" which links to AC data.
  </action>
  <verify>
    <automated>pnpm playwright test apps/frontend/tests/discovery/epic.spec.ts</automated>
  </verify>
  <done>EPIC search validates input and transitions to constituency view.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Manual Drill-down Backup</name>
  <files>apps/frontend/src/components/discovery/manual-drilldown.tsx</files>
  <action>
    - Implement 3-step select: State -> Parliamentary Constituency (PC) -> Assembly Constituency (AC).
    - Use shadcn/ui Select components.
    - Fetch list data from backend AC collection.
  </action>
  <verify>
    <automated>pnpm playwright test apps/frontend/tests/discovery/manual.spec.ts</automated>
  </verify>
  <done>User can manually select their AC and see its current election stage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser -> GPS API | User grants permission for location data. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | GPS Data | mitigate | Use HTTPS. Don't store precise lat/lng in persistent logs (only AC_ID). |
</threat_model>

<success_criteria>
- GPS discovery completes in < 3s on 4G.
- EPIC search UI provides visual feedback in < 100ms.
</success_criteria>

<output>
After completion, create `.planning/phases/02-Data-Pipeline-ECI-Sync/02-05-SUMMARY.md`
</output>
