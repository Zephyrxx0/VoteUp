---
phase: 02-Data-Pipeline-ECI-Sync
plan: 04
type: execute
wave: 2
depends_on: [02-03]
files_modified: [apps/backend/src/services/reporter/consensus.ts, apps/backend/src/services/db/reports-store.ts]
autonomous: true
requirements: [RPT-01]

must_haves:
  truths:
    - "User reports for election stage are weighted by trust scores"
    - "Consensus logic updates AC inferred_stage when threshold met"
  artifacts:
    - path: "apps/backend/src/services/reporter/consensus.ts"
      provides: "Trust-weighted consensus algorithm"
    - path: "apps/backend/src/services/db/reports-store.ts"
      provides: "User reporting persistence"
---

<objective>
Implement the community reporting system to handle official data gaps. This allows users to report the current election stage of their constituency, with a trust-weighted consensus logic to prevent spam/manipulation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-Data-Pipeline-ECI-Sync/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Community Stage Reporting API</name>
  <files>apps/backend/src/services/db/reports-store.ts, apps/backend/routes/reports.js</files>
  <action>
    - Create `reports` collection in Firestore.
    - Implement POST endpoint for submitting a stage report (requires auth).
    - Store `{ user_id, ac_id, reported_stage, timestamp, trust_score }`.
    - Fetch user's `trust_score` from their profile (default to 1.0 for new users).
  </action>
  <verify>
    <automated>pnpm jest apps/backend/src/services/db/reports-store.test.ts</automated>
  </verify>
  <done>User reports are successfully persisted with correct metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Consensus Calculation Logic</name>
  <files>apps/backend/src/services/reporter/consensus.ts</files>
  <action>
    - Implement `calculateConsensus(ac_id)` logic.
    - Group reports by stage within a 12h window.
    - Sum trust scores per stage.
    - If a stage exceeds the threshold (e.g., sum > 10.0 and > 60% of total), update `inferred_stage` in the AC document.
    - Setup Cloud Function trigger (Firestore `onWrite` to reports) to run consensus check.
  </action>
  <verify>
    <automated>pnpm jest apps/backend/src/services/reporter/consensus.test.ts</automated>
  </verify>
  <done>AC inferred_stage updates correctly when consensus is reached.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Auth User -> Reports API | External input from authenticated users. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Report Spam | mitigate | Rate limit reporting (1 report per user per constituency per 12h). |
| T-02-04-02 | Spoofing | Trust Score | mitigate | Calculate trust scores based on historical accuracy of user reports. |
</threat_model>

<success_criteria>
- Consensus algorithm correctly filters out minority "noise" reports.
- `inferred_stage` is updated within < 10 seconds of a threshold-crossing report.
</success_criteria>

<output>
After completion, create `.planning/phases/02-Data-Pipeline-ECI-Sync/02-04-SUMMARY.md`
</output>
