---
phase: 02-Data-Pipeline-ECI-Sync
plan: 07
type: execute
wave: 4
depends_on: [02-05]
files_modified: [apps/frontend/src/lib/offline-store.ts, apps/frontend/public/sw.js]
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "App displays last known good data when offline"
    - "Users can manually enter election dates in 'Offline Mode'"
    - "Stale data is visually indicated with a warning"
  artifacts:
    - path: "apps/frontend/src/lib/offline-store.ts"
      provides: "LocalStorage persistence for AC data"
    - path: "apps/frontend/public/sw.js"
      provides: "Service Worker for asset caching"
---

<objective>
Implement offline resilience and fallback mechanisms. This ensures users can still access election information even with poor connectivity, and can manually provide data if the official sync fails.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-Data-Pipeline-ECI-Sync/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Local Data Caching & Stale Indicators</name>
  <files>apps/frontend/src/lib/offline-store.ts, apps/frontend/src/components/ui/stale-warning.tsx</files>
  <action>
    - Implement `saveConstituencyToLocal(ac_id, data)` using LocalStorage or IndexedDB.
    - Implement `getCachedConstituency(ac_id)` with a fallback to cache if network fails.
    - Create `StaleWarning` component that shows when `last_synced_at` is > 24h old.
  </action>
  <verify>
    <automated>pnpm playwright test apps/frontend/tests/resilience/offline-cache.spec.ts</automated>
  </verify>
  <done>App successfully loads cached data in airplane mode.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Offline User-Input Mode</name>
  <files>apps/frontend/src/components/discovery/offline-input.tsx</files>
  <action>
    - Create a "Missing Data? Help us out" button in the UI.
    - Implement a form for users to manually enter their polling/counting dates if official data is missing.
    - Save this "user-provided" data to `offline-store.ts`.
    - Mark UI clearly as "Using your provided dates".
  </action>
  <verify>
    <automated>pnpm playwright test apps/frontend/tests/resilience/user-input.spec.ts</automated>
  </verify>
  <done>Users can manually set their election timeline when data is unavailable.</done>
</task>

<task type="auto">
  <name>Task 3: Setup Basic Service Worker for Asset Caching</name>
  <files>apps/frontend/public/sw.js, apps/frontend/src/app/layout.tsx</files>
  <action>
    - Register a standard Service Worker in `layout.tsx`.
    - Implement `install` and `fetch` handlers in `sw.js` to cache core assets (HTML, CSS, JS).
    - Ensure the pipeline UI works without a network connection.
  </action>
  <verify>
    <automated>pnpm playwright test apps/frontend/tests/resilience/sw.spec.ts</automated>
  </verify>
  <done>Core UI assets are cached and available offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> LocalStorage | Local storage is untrusted but user-specific. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | Local Cache | accept | User can only tamper with their own local cache; no system-wide impact. |
</threat_model>

<success_criteria>
- App remains functional and readable with 0% network connectivity (offline).
- Stale data warnings appear automatically after 24 hours of sync silence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-Data-Pipeline-ECI-Sync/02-07-SUMMARY.md`
</output>
