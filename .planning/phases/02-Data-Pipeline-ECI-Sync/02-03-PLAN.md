---
phase: 02-Data-Pipeline-ECI-Sync
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified: [apps/backend/src/cron/sync-scheduler.ts, apps/backend/src/services/db/constituency-store.ts]
autonomous: true
requirements: [SYNC-01, DATA-01]

must_haves:
  truths:
    - "ECI sync runs every 12 hours via Cloud Scheduler"
    - "Firestore AC collection reflects official schedule data with 8-stage states"
  artifacts:
    - path: "apps/backend/src/cron/sync-scheduler.ts"
      provides: "Cloud Function Cron trigger"
    - path: "apps/backend/src/services/db/constituency-store.ts"
      provides: "Firestore batch write logic"
---

<objective>
Automate the data sync process using Firebase Cloud Functions (Gen 2) and Cloud Scheduler. Persistent official election data in a granular Firestore collection for high-performance reading by the frontend.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-Data-Pipeline-ECI-Sync/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup 12h Cron Job (Cloud Functions Gen 2)</name>
  <files>apps/backend/src/cron/sync-scheduler.ts</files>
  <action>
    - Setup Firebase Cloud Functions (Gen 2) in apps/backend.
    - Implement `scheduledECISync` using `onSchedule` (or standard `cron` if using node-cron for local testing).
    - Set frequency to `0 */12 * * *` (every 12 hours).
    - Wire scheduler to the Scraper Engine (02-02).
  </action>
  <verify>
    <automated>pnpm jest apps/backend/src/cron/sync-scheduler.test.ts</automated>
  </verify>
  <done>Cron job triggers scraper logic successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Firestore AC Persistence Logic</name>
  <files>apps/backend/src/services/db/constituency-store.ts</files>
  <action>
    - Setup Firestore schema for `constituencies` collection.
    - Implement `upsertConstituencyData(data[])` using batch writes for efficiency.
    - Include `last_synced_at` and `source_url` metadata.
    - Implement `stale` data marking if sync fails for > 24 hours.
  </action>
  <verify>
    <automated>pnpm jest apps/backend/src/services/db/constituency-store.test.ts</automated>
  </verify>
  <done>Firestore collection contains normalized constituency data from scraper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Scraper -> Firestore | Internal data flow within backend trust zone. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | Sync Data | mitigate | Ensure Cloud Functions run with limited service account permissions (only write to specific collections). |
| T-02-03-02 | Repudiation | Sync Actions | mitigate | Log sync start/stop/fail events to Cloud Logging. |
</threat_model>

<success_criteria>
- End-to-end sync (Scrape -> Parse -> Save) completes in < 5 minutes for a typical state schedule.
- AC records in Firestore correctly reflect all 8 election stages per DATA-01.
</success_criteria>

<output>
After completion, create `.planning/phases/02-Data-Pipeline-ECI-Sync/02-03-SUMMARY.md`
</output>
