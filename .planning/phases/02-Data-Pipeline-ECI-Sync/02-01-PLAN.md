---
phase: 02-Data-Pipeline-ECI-Sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/backend/src/services/geo/mapper.ts, apps/backend/src/lib/validation.ts]
autonomous: true
requirements: [MAP-01, VAL-01]

must_haves:
  truths:
    - "GPS coordinates correctly map to specific AC/PC IDs"
    - "Voter EPIC numbers are validated via regex and Luhn checksum"
  artifacts:
    - path: "apps/backend/src/services/geo/mapper.ts"
      provides: "Turf.js point-in-polygon logic"
    - path: "apps/backend/src/lib/validation.ts"
      provides: "EPIC validation rules"
  key_links:
    - from: "GPS Input"
      to: "AC_ID"
      via: "turf.booleanPointInPolygon"
---

<objective>
Implement the core geo-spatial and validation logic needed for constituency discovery. This provides the mathematical foundation for identifying where a user votes based on their physical location or voter ID.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-Data-Pipeline-ECI-Sync/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Geo-spatial Mapping Service</name>
  <files>apps/backend/src/services/geo/mapper.ts, apps/backend/data/maps/india_ac_pc.json</files>
  <action>
    - Install turf: `pnpm add @turf/turf` in apps/backend.
    - Download/prepare DataMeet GeoJSON for AC/PC boundaries (use post-2008 delimitation per RESEARCH.md).
    - Implement `getConstituencyByCoords(lat, lng)` using `turf.booleanPointInPolygon`.
    - Handle edge cases where coordinates are outside India boundaries.
  </action>
  <verify>
    <automated>pnpm jest apps/backend/src/services/geo/mapper.test.ts</automated>
  </verify>
  <done>Service returns correct AC_ID for known coordinate pairs (e.g., Delhi, Mumbai).</done>
</task>

<task type="auto">
  <name>Task 2: Implement EPIC Validation Logic</name>
  <files>apps/backend/src/lib/validation.ts</files>
  <action>
    - Implement `validateEPIC(epic: string)` function.
    - Use Regex: `^[A-Z]{3}[0-9]{7}$` for modern IDs.
    - Implement Luhn (Mod 10) checksum but treat as a confidence score, not a hard rejection (per Pitfall 3 in RESEARCH.md).
    - Return object `{ valid: boolean, confidence: number, type: 'modern' | 'legacy' }`.
  </action>
  <verify>
    <automated>pnpm jest apps/backend/src/lib/validation.test.ts</automated>
  </verify>
  <done>Validation correctly identifies valid/invalid EPIC formats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted GPS coordinates and EPIC strings cross here. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | GPS Input | mitigate | Validate coordinate ranges (Lat -90 to 90, Lng -180 to 180) using Zod. |
| T-02-01-02 | Spoofing | EPIC Search | mitigate | Rate limit EPIC lookups to prevent brute-force discovery of voter IDs. |
</threat_model>

<success_criteria>
- Turf.js mapping returns AC_ID within < 100ms for single point.
- EPIC validation logic handles both 10-digit modern and varying-length legacy formats correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-Data-Pipeline-ECI-Sync/02-01-SUMMARY.md`
</output>
