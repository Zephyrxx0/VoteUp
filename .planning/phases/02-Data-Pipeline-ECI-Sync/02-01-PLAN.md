---
phase: 02-Data-Pipeline-ECI-Sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/backend/package.json, apps/backend/tsconfig.json, apps/backend/src/app.ts, apps/backend/src/routes/geo.ts, apps/backend/src/services/geo/mapper.ts, apps/backend/src/lib/validation.ts, apps/frontend/src/lib/validation.ts]
autonomous: true
requirements: [MAP-01, VAL-01]

must_haves:
  truths:
    - "Backend running on TypeScript with src/ directory structure"
    - "GPS coordinates correctly map to specific AC/PC IDs via API route"
    - "Voter EPIC numbers validated via shared logic in both frontend and backend"
  artifacts:
    - path: "apps/backend/tsconfig.json"
      provides: "TS configuration"
    - path: "apps/backend/src/routes/geo.ts"
      provides: "Geo-mapping API endpoint"
    - path: "apps/backend/src/lib/validation.ts"
      provides: "Backend EPIC validation"
    - path: "apps/frontend/src/lib/validation.ts"
      provides: "Frontend EPIC validation"
---

<objective>
Migrate backend to TypeScript and implement core geo-spatial/validation logic. This standardizes the stack and provides the foundation for constituency discovery.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-Data-Pipeline-ECI-Sync/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate apps/backend to TypeScript</name>
  <files>apps/backend/package.json, apps/backend/tsconfig.json, apps/backend/src/app.ts, apps/backend/src/bin/www.ts</files>
  <action>
    - Install TS dependencies: `pnpm add -D typescript ts-node @types/node @types/express @types/cookie-parser @types/morgan @types/debug` in apps/backend.
    - Create `tsconfig.json` with `rootDir: "src"` and `outDir: "dist"`.
    - Create `src/` directory.
    - Move and rename: `app.js` -> `src/app.ts`, `routes/` -> `src/routes/`.
    - Update `src/app.ts` and routes to use ESM `import/export`.
    - Convert `bin/www` to `src/bin/www.ts` or update `package.json` to run `ts-node src/app.ts`.
  </action>
  <verify>
    <automated>pnpm -C apps/backend exec tsc --noEmit</automated>
  </verify>
  <done>Backend compiles without errors and starts via ts-node.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Geo-spatial Mapping Service and Route</name>
  <files>apps/backend/src/services/geo/mapper.ts, apps/backend/src/routes/geo.ts, apps/backend/data/maps/india_ac_pc.json</files>
  <action>
    - Install turf: `pnpm add @turf/turf`.
    - Download/prepare DataMeet GeoJSON for AC/PC boundaries (post-2008).
    - Implement `getConstituencyByCoords(lat, lng)` in `mapper.ts`.
    - Create `src/routes/geo.ts` with POST `/api/geo/map` endpoint.
    - Register route in `src/app.ts`.
  </action>
  <verify>
    <automated>pnpm jest apps/backend/src/services/geo/mapper.test.ts</automated>
  </verify>
  <done>API returns correct AC_ID for coordinate pairs (e.g., Delhi, Mumbai).</done>
</task>

<task type="auto">
  <name>Task 3: Implement Shared EPIC Validation Logic</name>
  <files>apps/backend/src/lib/validation.ts, apps/frontend/src/lib/validation.ts</files>
  <action>
    - Implement `validateEPIC(epic: string)` with Regex: `^[A-Z]{3}[0-9]{7}$` and Luhn checksum.
    - Duplicate logic in both files (per instructions, to avoid monorepo linking complexity for now).
    - Return `{ valid: boolean, confidence: number, type: 'modern' | 'legacy' }`.
  </action>
  <verify>
    <automated>pnpm jest apps/backend/src/lib/validation.test.ts && pnpm jest apps/frontend/src/lib/validation.test.ts</automated>
  </verify>
  <done>Both apps correctly validate EPIC formats using identical logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted GPS coordinates and EPIC strings. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | GPS Input | mitigate | Validate range (Lat -90/90, Lng -180/180) with Zod in geo route. |
| T-02-01-02 | Spoofing | EPIC Search | mitigate | Rate limit EPIC lookups. |
</threat_model>

<success_criteria>
- Backend uses TypeScript and `src/` structure.
- Geo API returns AC_ID in < 100ms.
- EPIC validation logic works in both frontend and backend.
</success_criteria>

<output>
After completion, create `.planning/phases/02-Data-Pipeline-ECI-Sync/02-01-SUMMARY.md`
</output>
