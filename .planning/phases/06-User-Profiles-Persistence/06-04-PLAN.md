---
phase: 06-User-Profiles-Persistence
plan: 04
type: execute
wave: 2
depends_on: [06-02, 06-03]
files_modified: [apps/frontend/src/components/dashboard/ProfileView.tsx, apps/frontend/src/app/[locale]/dashboard/profile/page.tsx]
autonomous: false
requirements: [PRF-03]

must_haves:
  truths:
    - "User can view their earned badges in the profile."
    - "User can see a summary of their completed election stages."
    - "Account sync status (Anonymous vs Persistent) is clearly indicated."
  artifacts:
    - path: "apps/frontend/src/components/dashboard/ProfileView.tsx"
      provides: "User profile dashboard section"
---

<objective>
Create a dedicated Profile view in the dashboard to manage account settings and view progress.

Purpose: Provides users with a central place to see their journey history and persistence status.
Output: A new "Profile" page and supporting UI components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/frontend/src/app/[locale]/dashboard/page.tsx
@.planning/phases/06-User-Profiles-Persistence/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProfileView component</name>
  <files>apps/frontend/src/components/dashboard/ProfileView.tsx</files>
  <action>
    Create a `ProfileView` component that:
    1. Displays user's earned badges (mapping IDs to visual assets/icons).
    2. Lists completed election stages from the `history` field.
    3. Shows sync status: "Local Only" (anonymous) or "Cloud Synced" (Google-linked).
    4. Includes the `UpgradePrompt` from Plan 03 if anonymous.
    Use `lucide-react` for badge icons and layout consistent with the Dashboard.
  </action>
  <verify>
    Component renders without errors and correctly displays mock data for history/badges.
  </verify>
  <done>Profile section is ready for integration.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Profile page into Dashboard</name>
  <files>apps/frontend/src/app/[locale]/dashboard/profile/page.tsx</files>
  <action>
    Create the Next.js page for the profile route. 
    Ensure it's wrapped in necessary auth/context providers.
    Add a link to this page from the main Dashboard navigation.
  </action>
  <verify>
    Navigating to `/dashboard/profile` displays the `ProfileView`.
  </verify>
  <done>User profile is accessible via the dashboard navigation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Profile View UI with badges and history list.</what-built>
  <how-to-verify>
    1. Login to the app.
    2. Navigate to Dashboard -> Profile.
    3. Verify that badges (if any) and history are displayed correctly.
    4. Verify the sync status indicator reflects the current auth state.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → User | Displaying sensitive info (though here it's just summaries) to the correct user only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | Profile Data | mitigate | Ensure the profile page is protected by auth middleware and only fetches data for the logged-in UID. |
</threat_model>

<verification>
Check navigation link and page rendering.
</verification>

<success_criteria>
- Profile page correctly identifies the user's current progress.
- Visual feedback is provided for "upgraded" status.
- UI is responsive and follows the project's design language.
</success_criteria>

<output>
After completion, create `.planning/phases/06-User-Profiles-Persistence/06-04-SUMMARY.md`
</output>
