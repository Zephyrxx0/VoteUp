---
phase: 06-User-Profiles-Persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [packages/contracts/src/user.ts, apps/frontend/src/lib/user-service.ts]
autonomous: true
requirements: [PRF-01, PRF-03]

must_haves:
  truths:
    - "User profile schema exists with history, badges, and encrypted data fields."
    - "Firestore reads/writes for user profiles are restricted to the owner."
  artifacts:
    - path: "packages/contracts/src/user.ts"
      provides: "Zod schema and TypeScript types for User Profile"
    - path: "apps/frontend/src/lib/user-service.ts"
      provides: "CRUD operations for Firestore user collection"
---

<objective>
Define the User Profile schema and implement a service for Firestore operations.

Purpose: Establishes the data structure and storage logic for persistent user profiles.
Output: Contracts for user data and a frontend service to manage profile lifecycle.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/contracts/src/checklist.ts
@apps/frontend/src/lib/firebase.ts
@.planning/phases/06-User-Profiles-Persistence/06-RESEARCH.md
</context>

<interfaces>
Target interface in packages/contracts/src/user.ts:
```typescript
import { z } from 'zod';

export const UserProfileSchema = z.object({
  history: z.array(z.string()), // Stage IDs
  badges: z.array(z.string()),  // Badge IDs
  encryptedVoterData: z.object({
    ciphertext: z.string(),
    iv: z.string(),
    salt: z.string()
  }).optional(),
  updatedAt: z.any() // Firestore Timestamp
});

export type UserProfile = z.infer<typeof UserProfileSchema>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define User Profile contract</name>
  <files>packages/contracts/src/user.ts</files>
  <action>
    Create the UserProfile schema using Zod as defined in the interfaces section. 
    Ensure it aligns with D-06-01 (Stage-level history only).
    Export both the Zod schema and the inferred TypeScript type.
  </action>
  <verify>
    File exists and `pnpm -F contracts build` passes.
  </verify>
  <done>UserProfile contract is available for use in the frontend.</done>
</task>

<task type="auto">
  <name>Task 2: Implement User Service for Firestore</name>
  <files>apps/frontend/src/lib/user-service.ts, apps/frontend/src/lib/user-service.test.ts</files>
  <action>
    Create a service to manage the `users` collection in Firestore.
    1. Implement `getUserProfile(uid)`: Fetches a profile.
    2. Implement `updateUserProfile(uid, data)`: Merges new data into the profile.
    3. Implement `createInitialProfile(uid)`: Sets up a default profile for new users.
    4. Create `user-service.test.ts` to verify CRUD logic (mocking Firestore).
  </action>
  <verify>
    <automated>pnpm -F frontend vitest run user-service.test.ts</automated>
  </verify>
  <done>Frontend can interact with the Firestore users collection with verified logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Firestore | Only the authenticated user should be able to read/write their own document. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | Firestore collection | mitigate | Implement Firestore Security Rules: `match /users/{uid} { allow read, write: if request.auth.uid == uid }`. |
| T-06-02-02 | Elevation of Privilege | User ID spoofing | mitigate | Firebase Auth automatically enforces `request.auth.uid` based on the secure token. |
</threat_model>

<verification>
Check for existence of `packages/contracts/src/user.ts` and `apps/frontend/src/lib/user-service.ts`.
</verification>

<success_criteria>
- UserProfile type includes `history: string[]` and `badges: string[]`.
- `user-service.ts` correctly initializes Firestore and uses the current user's UID for document IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-User-Profiles-Persistence/06-02-SUMMARY.md`
</output>
