---
phase: 06-User-Profiles-Persistence
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified: [apps/frontend/src/lib/auth.ts, apps/frontend/src/components/auth/UpgradePrompt.tsx]
autonomous: true
requirements: [PRF-02]

must_haves:
  truths:
    - "Anonymous users can link their session to a Google account without losing progress."
    - "The app correctly identifies if a user is 'upgraded' (Google-linked) vs 'anonymous'."
  artifacts:
    - path: "apps/frontend/src/components/auth/UpgradePrompt.tsx"
      provides: "UI banner/modal for account linking"
---

<objective>
Implement Google account linking to upgrade anonymous sessions to persistent user profiles.

Purpose: Enables cross-device badge persistence and data security as per D-06-03.
Output: Enhanced auth logic and a UI component to trigger the upgrade flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/frontend/src/lib/auth.ts
@.planning/phases/06-User-Profiles-Persistence/06-RESEARCH.md
</context>

<interfaces>
From apps/frontend/src/lib/auth.ts:
```typescript
export async function upgradeToGoogle(): Promise<{ user: User | null; error: Error | null }>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enhance auth logic for account linking</name>
  <files>apps/frontend/src/lib/auth.ts</files>
  <action>
    Update `apps/frontend/src/lib/auth.ts` to include a proper account linking function.
    Use `linkWithPopup(auth.currentUser, provider)` instead of `signInWithPopup` to ensure the anonymous session data is preserved and associated with the Google account.
    Handle errors like `auth/credential-already-in-use` by providing a clear message (merging logic is deferred for now, just notify user).
  </action>
  <verify>
    `linkGoogleAccount` (or new `upgradeToGoogle`) is implemented and uses `linkWithPopup`.
  </verify>
  <done>Backend logic for account upgrading is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Create UpgradePrompt UI component</name>
  <files>apps/frontend/src/components/auth/UpgradePrompt.tsx</files>
  <action>
    Create a `UpgradePrompt` component (Banner or Modal) that appears for anonymous users who have earned at least one badge or completed one stage.
    It should explain the benefits (persistence, cross-device sync) and have a "Link Google Account" button.
    Use `lucide-react` for icons and shadcn components for the UI.
  </action>
  <verify>
    Component exists and calls the upgrade function on button click.
  </verify>
  <done>Users have a clear path to upgrade their accounts from the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Google OAuth | The auth flow transitions to Google's trusted domain and back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | Auth Token | mitigate | Use Firebase's secure `linkWithPopup` which handles tokens according to best practices. |
| T-06-03-02 | Denial of Service | Linking Loop | mitigate | Prevent multiple concurrent linking attempts with a loading state in the UI. |
</threat_model>

<verification>
Check if `UpgradePrompt` is imported and used in a relevant view (e.g., Dashboard or Onboarding).
</verification>

<success_criteria>
- `auth.ts` implements `linkWithPopup`.
- `UpgradePrompt` is visually consistent with the rest of the app.
- Successfully linking a Google account preserves the original Firebase UID (upgraded).
</success_criteria>

<output>
After completion, create `.planning/phases/06-User-Profiles-Persistence/06-03-SUMMARY.md`
</output>
