---
phase: 06-User-Profiles-Persistence
plan: 03
type: execute
wave: 2
depends_on: [06-02]
files_modified: [apps/frontend/src/lib/auth.ts, apps/frontend/src/components/auth/UpgradePrompt.tsx, apps/frontend/src/lib/auth-linking.test.ts]
autonomous: true
requirements: [PRF-02]

must_haves:
  truths:
    - "Anonymous users can link their session to a Google account without losing progress."
    - "The app handles 'Merge & Sync' when the Google account already has an existing profile."
    - "Constituency mismatches during merging are handled via a user prompt (deferred to UI logic)."
  artifacts:
    - path: "apps/frontend/src/lib/auth-linking.test.ts"
      provides: "Unit tests for account linking and merging logic"
---

<objective>
Implement Google account linking and profile merging logic to upgrade anonymous sessions.

Purpose: Enables cross-device badge persistence and ensures existing cloud data is merged with local progress.
Output: Enhanced auth service with merge logic and a UI component to trigger the flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/frontend/src/lib/auth.ts
@apps/frontend/src/lib/user-service.ts
@.planning/phases/06-User-Profiles-Persistence/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Account Linking with Merge Logic</name>
  <files>apps/frontend/src/lib/auth.ts, apps/frontend/src/lib/auth-linking.test.ts</files>
  <action>
    Update `apps/frontend/src/lib/auth.ts` to implement `upgradeToGoogle`.
    1. Use `linkWithPopup` from Firebase.
    2. Catch `auth/credential-already-in-use`.
    3. If caught, implement the **Merge & Sync** strategy:
       - Fetch the existing Google profile using `getUserProfile`.
       - Merge local anonymous `history` and `badges` into the cloud profile (unique union).
       - Sign in to the Google account and update the cloud profile with the merged data.
    4. Create `auth-linking.test.ts` and mock Firebase Auth/Firestore to verify the merge logic.
  </action>
  <verify>
    <automated>pnpm -F frontend vitest run auth-linking.test.ts</automated>
  </verify>
  <done>Linking logic handles both new upgrades and merges with existing cloud profiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create Merge Conflict UI</name>
  <files>apps/frontend/src/components/auth/MergeConflictDialog.tsx</files>
  <action>
    Create a `MergeConflictDialog` component (shadcn AlertDialog).
    - Trigger: Show when `upgradeToGoogle` detects a constituency mismatch between local and cloud profiles.
    - Options: "Keep Cloud Data" vs "Overwrite with Local Progress".
    - Action: Return the user's choice to the linking flow.
  </action>
  <verify>
    Component renders and returns correct choice in a mock test.
  </verify>
  <done>Users can resolve data conflicts during account linking.</done>
</task>

<task type="auto">
  <name>Task 3: Create UpgradePrompt UI component</name>
  <files>apps/frontend/src/components/auth/UpgradePrompt.tsx</files>
  <action>
    Create a `UpgradePrompt` component (Banner or Modal) that appears for anonymous users.
    - Trigger: Show if the user is anonymous and has earned at least 1 badge.
    - UI: Explain benefits of syncing (D-06-03).
    - Interaction: Call `upgradeToGoogle` on click.
    - Feedback: Show success/error toasts using `sonner` or `toast`.
  </action>
  <verify>
    Component exists and triggers the linking flow.
  </verify>
  <done>Users are proactively prompted to secure their civic progress via Google linking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User â†’ Google OAuth | The auth flow transitions to Google's trusted domain and back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | Auth Token | mitigate | Use Firebase's secure `linkWithPopup`. |
| T-06-03-02 | Data Loss | Merging Logic | mitigate | Always perform a union of local/cloud data; never overwrite without a prompt if constituencies mismatch (handled in UI). |
</threat_model>

<verification>
Run `pnpm -F frontend vitest auth-linking.test.ts`.
</verification>

<success_criteria>
- `upgradeToGoogle` successfully links accounts.
- Merging logic results in a union of badges and history from both profiles.
- `UpgradePrompt` appears correctly based on anonymous state.
</success_criteria>

<output>
After completion, create `.planning/phases/06-User-Profiles-Persistence/06-03-SUMMARY.md`
</output>
