---
phase: 06-User-Profiles-Persistence
plan: 06
type: execute
wave: 3
depends_on: [06-02, 06-04]
files_modified: [apps/frontend/src/lib/user-service.ts, apps/frontend/src/components/dashboard/PrivacySettings.tsx]
autonomous: false
requirements: [PRF-01]

must_haves:
  truths:
    - "Users can delete all their data (Firestore document and local state)."
    - "A confirmation step is required before data deletion."
  artifacts:
    - path: "apps/frontend/src/components/dashboard/PrivacySettings.tsx"
      provides: "UI for data deletion and privacy controls"
---

<objective>
Implement privacy controls, specifically the "Delete All My Data" feature for compliance and user trust.

Purpose: Ensures user agency over their data as per project privacy principles.
Output: A deletion service and a settings UI to trigger it.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/frontend/src/lib/user-service.ts
@.planning/phases/06-User-Profiles-Persistence/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement data deletion logic</name>
  <files>apps/frontend/src/lib/user-service.ts</files>
  <action>
    Implement `deleteAllUserData(uid)` in `user-service.ts`.
    1. Delete the user's document from the `users` collection.
    2. Clear local Zustand stores.
    3. (Optional) Log out the user or reset to anonymous state.
  </action>
  <verify>
    Service export `deleteAllUserData` is available and performs document deletion.
  </verify>
  <done>Data deletion logic is ready for UI integration.</done>
</task>

<task type="auto">
  <name>Task 2: Create PrivacySettings UI</name>
  <files>apps/frontend/src/components/dashboard/PrivacySettings.tsx</files>
  <action>
    Create a `PrivacySettings` component that:
    1. Provides a "Delete All My Data" button.
    2. Shows a confirmation dialog (using shadcn Alert Dialog) explaining that this action is irreversible.
    3. Triggers the deletion logic upon confirmation.
    Integrate this component into the `ProfileView`.
  </action>
  <verify>
    Button exists and shows a confirmation dialog before proceeding.
  </verify>
  <done>Privacy settings are accessible in the user profile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Data deletion feature with confirmation dialog.</what-built>
  <how-to-verify>
    1. Go to Profile -> Privacy Settings.
    2. Click "Delete All My Data".
    3. Verify the confirmation dialog appears.
    4. Confirm deletion.
    5. Verify (via Firebase Console) that the user document is gone.
    6. Verify that local state is reset.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Data Destruction | High-stakes boundary where intentionality must be verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | Deletion Request | mitigate | Use a confirmation dialog and ensure the request is authenticated for the specific UID. |
| T-06-06-02 | Denial of Service | Accidental Deletion | mitigate | Clear warning text and a non-destructive default button in the confirmation dialog. |
</threat_model>

<verification>
Check deletion flow and Firestore state.
</verification>

<success_criteria>
- User document is successfully removed from Firestore.
- User session is reset or logged out.
- UI provides clear feedback after deletion completes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-User-Profiles-Persistence/06-06-SUMMARY.md`
</output>
