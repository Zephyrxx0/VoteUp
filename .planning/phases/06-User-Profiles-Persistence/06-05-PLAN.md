---
phase: 06-User-Profiles-Persistence
plan: 05
type: tdd
wave: 3
depends_on: [06-01, 06-02, 06-03]
files_modified: [apps/frontend/src/components/shared/FirestoreSync.tsx, apps/frontend/src/components/shared/FirestoreSync.test.tsx]
autonomous: true
requirements: [PRF-01, PRF-02, PRF-03]

must_haves:
  truths:
    - "Zustand state changes trigger a Firestore sync for upgraded users."
    - "Sensitive voter data is encrypted before being sent to Firestore during sync."
    - "Sync operations are debounced to prevent excessive Firestore writes."
  artifacts:
    - path: "apps/frontend/src/components/shared/FirestoreSync.tsx"
      provides: "Zustand-to-Firestore synchronization logic"
  key_links:
    - from: "apps/frontend/src/components/shared/FirestoreSync.tsx"
    - to: "apps/frontend/src/lib/encryption.ts"
      via: "encryptSensitive call"
---

<objective>
Implement the FirestoreSync component to bridge Zustand state and cloud persistence with encryption.

Purpose: Automates the persistence of local user progress to the cloud for persistent accounts, ensuring privacy via client-side encryption.
Output: A background sync component that maintains data consistency.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/frontend/src/lib/encryption.ts
@apps/frontend/src/lib/user-service.ts
@apps/frontend/src/lib/stores/onboarding-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FirestoreSync logic</name>
  <files>apps/frontend/src/components/shared/FirestoreSync.tsx, apps/frontend/src/components/shared/FirestoreSync.test.tsx</files>
  <behavior>
    - Test 1: Sync does NOT run if user is anonymous.
    - Test 2: Sync runs when relevant state (badges, history) changes for persistent users.
    - Test 3: Sensitive fields (EPIC, AC ID) are encrypted using `encryptSensitive` before calling `updateUserProfile`.
    - Test 4: Multiple rapid state changes result in only one Firestore write (debouncing).
  </behavior>
  <action>
    Create a `FirestoreSync` component (rendered at the root) that:
    1. Subscribes to relevant Zustand stores (Onboarding, Checklists).
    2. Checks if `auth.currentUser` is not null and NOT anonymous.
    3. Transforms the state into the `UserProfile` format.
    4. Encrypts sensitive fields if they exist in the state.
    5. Calls `user-service.ts` to update Firestore.
    Use `useEffect` or Zustand's `subscribe` with a debounce timer.
  </action>
  <verify>
    <automated>pnpm -F frontend test FirestoreSync.test.tsx</automated>
  </verify>
  <done>Automated sync logic is verified and handles encryption correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand (Local) → Firestore (Cloud) | Transition point where encryption must occur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | Network Traffic | mitigate | Sensitive data is encrypted before sync (D-06-04). |
| T-06-05-02 | Resource Exhaustion | Firestore Quota | mitigate | Use debouncing (e.g., 2-second wait) to prevent excessive writes on every keystroke. |
</threat_model>

<verification>
Check for `FirestoreSync` component being rendered in the main `Layout.tsx` or similar.
</verification>

<success_criteria>
- Sync only happens for non-anonymous users.
- `updateUserProfile` receives an `encryptedVoterData` object for sensitive fields.
- Firestore writes are observable in the Firebase Console (during manual verification).
</success_criteria>

<output>
After completion, create `.planning/phases/06-User-Profiles-Persistence/06-05-SUMMARY.md`
</output>
