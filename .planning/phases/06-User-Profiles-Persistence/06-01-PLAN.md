---
phase: 06-User-Profiles-Persistence
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [apps/frontend/src/lib/encryption.ts, apps/frontend/src/lib/encryption.test.ts]
autonomous: true
requirements: [PRF-01]

must_haves:
  truths:
    - "Sensitive data (EPIC, AC ID) is encrypted before being stored."
    - "Encryption keys are unique per user and never stored in plain text."
    - "Data can be successfully decrypted using the same user session seed."
  artifacts:
    - path: "apps/frontend/src/lib/encryption.ts"
      provides: "AES-GCM encryption/decryption functions"
  key_links:
    - from: "apps/frontend/src/lib/encryption.ts"
      to: "Web Crypto API"
      via: "window.crypto.subtle"
---

<objective>
Implement a client-side encryption utility using the Web Crypto API to protect sensitive voter data.

Purpose: Ensures that voter identifiers (like EPIC numbers) are unreadable by the server or anyone with database access, maintaining user privacy as per D-06-04.
Output: A robust, tested encryption library in the frontend.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/frontend/src/lib/auth.ts
@.planning/phases/06-User-Profiles-Persistence/06-RESEARCH.md
</context>

<interfaces>
From apps/frontend/src/lib/encryption.ts (target):
```typescript
export interface EncryptedData {
  ciphertext: string; // Base64
  iv: string;         // Base64
  salt: string;       // Base64
}

export async function encryptSensitive(uid: string, data: string): Promise<EncryptedData>;
export async function decryptSensitive(uid: string, encrypted: EncryptedData): Promise<string>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AES-GCM encryption utility</name>
  <files>apps/frontend/src/lib/encryption.ts, apps/frontend/src/lib/encryption.test.ts</files>
  <behavior>
    - Test 1: Encryption returns ciphertext, IV, and salt as base64 strings.
    - Test 2: Decryption of valid ciphertext returns original string.
    - Test 3: Decryption with wrong UID/seed fails (throws error).
    - Test 4: Encryption is deterministic for the same input and generated salt (via mock).
  </behavior>
  <action>
    Implement encryption logic using Web Crypto API. 
    1. Use PBKDF2 for key derivation from UID + random salt.
    2. Use AES-GCM 256-bit for encryption/decryption.
    3. Iterations for PBKDF2 should be at least 100,000.
    4. Ensure all binary data is converted to/from Base64 for storage compatibility.
  </action>
  <verify>
    <automated>pnpm -F frontend test encryption.test.ts</automated>
  </verify>
  <done>Encryption and decryption pass all unit tests including edge cases for key derivation failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Firestore | Sensitive data must be encrypted before crossing this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | Firestore storage | mitigate | Encrypt EPIC/AC IDs client-side using user-specific keys (D-06-04). |
| T-06-01-02 | Tampering | Encrypted payload | mitigate | Use AES-GCM which provides authenticated encryption (GCM tag check). |
</threat_model>

<verification>
Run `pnpm -F frontend test` and ensure `encryption.test.ts` passes.
</verification>

<success_criteria>
- encryption.ts exports `encryptSensitive` and `decryptSensitive`.
- Tests prove that data encrypted with UID 'A' cannot be decrypted with UID 'B'.
- Ciphertext is verified to be non-plain text (binary-encoded base64).
</success_criteria>

<output>
After completion, create `.planning/phases/06-User-Profiles-Persistence/06-01-SUMMARY.md`
</output>
