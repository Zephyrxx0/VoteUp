---
phase: 03-personalized-action-layer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/backend/src/services/checklist/ai-customizer.ts
  - apps/backend/src/routes/checklist.ts
autonomous: true
requirements:
  - ACT-01
must_haves:
  truths:
    - "Gemini 1.5 Pro grounds checklist templates with constituency-specific data"
    - "API returns customized checklists with real-world deadlines"
  artifacts:
    - path: "apps/backend/src/services/checklist/ai-customizer.ts"
      provides: "AI grounding logic using Gemini"
  key_links:
    - from: "apps/backend/src/services/checklist/ai-customizer.ts"
      to: "apps/backend/src/services/checklist/template-provider.ts"
      via: "Method call to fetch static templates"
    - from: "apps/backend/src/routes/checklist.ts"
      to: "apps/backend/src/services/checklist/ai-customizer.ts"
      via: "Method call to customize templates"
---

<objective>
Implement the AI customization layer using Gemini 1.5 Pro to ground the static checklist templates with localized, constituency-specific details like deadlines and office addresses.

Purpose: Delivers the "Personalized" aspect of the Action Layer (D-01).
Output: AI customization service and updated API endpoint.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-personalized-action-layer/03-CONTEXT.md
@.planning/phases/03-personalized-action-layer/03-RESEARCH.md
@.planning/phases/03-personalized-action-layer/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AI Customizer Service</name>
  <files>apps/backend/src/services/checklist/ai-customizer.ts</files>
  <action>
    Create `apps/backend/src/services/checklist/ai-customizer.ts`.
    Implement a class `AICustomizer` that uses `@google/generative-ai` to ground a `ChecklistTemplate`.
    - Inject the template JSON into a prompt.
    - Ask Gemini to update `description` and `title` fields with real-world dates and locations for a given `constituency` and `stage`.
    - Use system instructions to enforce the "Simple Instructions" rule (D-03).
    - Handle Gemini errors by falling back to the original static template.
  </action>
  <verify>
    <automated>pnpm -F backend test tests/ai-customizer.test.ts</automated>
  </verify>
  <done>AICustomizer returns a valid, customized ChecklistTemplate JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Customization Endpoint</name>
  <files>apps/backend/src/routes/checklist.ts</files>
  <action>
    Add `POST /api/checklist/customize` to `apps/backend/src/routes/checklist.ts`.
    The endpoint should accept `{ stage: number, constituency: string }` and return the customized checklist.
    It should first fetch the static template from `TemplateProvider`, then pass it to `AICustomizer`.
    Implement rate limiting for this endpoint to prevent excessive AI costs.
  </action>
  <verify>
    <automated>curl -X POST -H "Content-Type: application/json" -d '{"stage":1, "constituency":"Chennai South"}' http://localhost:3001/api/checklist/customize</automated>
  </verify>
  <done>Endpoint returns a customized JSON response for valid inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Ã¢â€ â€™ Gemini | Internal backend calls to External AI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | R | Gemini API Usage | mitigate | Implement rate limiting and logging of AI requests to detect abuse. |
| T-03-02-02 | I | AI Grounding Data | accept | AI uses public search data for grounding; no PII is sent to Gemini. |
| T-03-02-03 | T | AI Response Injection | mitigate | Use Zod to strictly validate the AI's JSON output before returning it to the client. |
</threat_model>

<success_criteria>
- AI-customized checklists are generated and returned via API.
- Fallback logic ensures reliability if AI fails.
</success_criteria>

<output>
After completion, create `.planning/phases/03-personalized-action-layer/03-02-SUMMARY.md`
</output>
