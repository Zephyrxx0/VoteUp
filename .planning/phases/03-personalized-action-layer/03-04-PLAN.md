---
phase: 03-personalized-action-layer
plan: 04
type: execute
wave: 2
depends_on: ["03-02", "03-03"]
files_modified:
  - apps/frontend/src/components/checklist/ChecklistContainer.tsx
  - apps/frontend/src/components/checklist/ChecklistItem.tsx
  - apps/frontend/src/app/[locale]/dashboard/page.tsx
autonomous: false
requirements:
  - ACT-01
must_haves:
  truths:
    - "User can view a personalized checklist on the dashboard"
    - "User can toggle items and see progress updated in the UI"
    - "Uncompleted items from previous stages are flagged as OVERDUE"
  artifacts:
    - path: "apps/frontend/src/components/checklist/ChecklistContainer.tsx"
      provides: "Main checklist layout and logic"
    - path: "apps/frontend/src/app/[locale]/dashboard/page.tsx"
      provides: "Dashboard shell"
---

<objective>
Build the checklist UI components and integrate them into a new Dashboard page, implementing the "Persistent Tasks" logic (D-04) to show uncompleted items from earlier stages.

Purpose: Delivers the user-facing interface for the Action Layer.
Output: Checklist UI components and a functional Dashboard integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-personalized-action-layer/03-CONTEXT.md
@.planning/phases/03-personalized-action-layer/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Checklist Components</name>
  <files>apps/frontend/src/components/checklist/ChecklistItem.tsx, apps/frontend/src/components/checklist/ChecklistContainer.tsx</files>
  <action>
    1. Create `ChecklistItem.tsx`: A functional component that renders a single task with a checkbox, title, and description. Use Lucide icons for status (e.g., `Circle`, `CheckCircle2`, `AlertCircle` for overdue).
    2. Create `ChecklistContainer.tsx`: 
       - Fetches customized checklist from `/api/checklist/customize`.
       - Merges current stage items with overdue items from previous stages (logic for D-04).
       - Maps over items and renders `ChecklistItem`.
       - Uses `useChecklistStore` to handle state.
    Apply the "Simple Instructions" rule (D-03) for the UI layout.
  </action>
  <verify>
    <automated>pnpm -F frontend test apps/frontend/src/components/checklist</automated>
  </verify>
  <done>Checklist components render correctly and handle item toggling.</done>
</task>

<task type="auto">
  <name>Task 2: Create Dashboard Page</name>
  <files>apps/frontend/src/app/[locale]/dashboard/page.tsx</files>
  <action>
    Create `apps/frontend/src/app/[locale]/dashboard/page.tsx`.
    - Implement a basic dashboard layout with a "Personalized Action Plan" section.
    - Embed `ChecklistContainer` in this section.
    - Add a header with the user's current election stage and constituency (use mock data if the global pipeline state isn't wired yet).
    Ensure the page is responsive and follows the mobile-first design pattern.
  </action>
  <verify>
    <automated>pnpm -F frontend test apps/frontend/src/app/[locale]/dashboard</automated>
  </verify>
  <done>Dashboard page created with integrated checklist.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual Verification of Checklist</name>
  <what-built>Checklist UI on the Dashboard with AI-customized content and overdue flagging.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and visit `http://localhost:3000/en/dashboard`.
    2. Verify the checklist displays items relevant to the current stage.
    3. Toggle an item and refresh the page to verify persistence.
    4. Simulate an "Overdue" task by manually adding an uncompleted item from stage 1 to the store and verify it is flagged in the UI.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client â†’ API | Frontend fetches AI-customized content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | S | Dashboard Page | mitigate | Ensure the page is protected by auth middleware (redirect to Home/Onboarding if no UID). |
</threat_model>

<success_criteria>
- User can interact with the checklist on the dashboard.
- Overdue items are clearly distinguishable.
- UI is intuitive and follows design guidelines.
</success_criteria>

<output>
After completion, create `.planning/phases/03-personalized-action-layer/03-04-SUMMARY.md`
</output>
