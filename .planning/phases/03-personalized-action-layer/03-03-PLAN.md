---
phase: 03-personalized-action-layer
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/frontend/src/lib/stores/checklist-store.ts
  - apps/frontend/src/lib/sync/checklist-sync.ts
autonomous: true
requirements:
  - ACT-01
must_haves:
  truths:
    - "Checklist progress is saved locally using Zustand persist"
    - "Progress syncs to Firestore in the background"
    - "User can toggle checklist items offline"
  artifacts:
    - path: "apps/frontend/src/lib/stores/checklist-store.ts"
      provides: "Zustand store with persistence"
    - path: "apps/frontend/src/lib/sync/checklist-sync.ts"
      provides: "Firestore sync service"
  key_links:
    - from: "apps/frontend/src/lib/sync/checklist-sync.ts"
      to: "Firestore"
      via: "setDoc with merge: true"
    - from: "apps/frontend/src/lib/stores/checklist-store.ts"
      to: "apps/frontend/src/lib/sync/checklist-sync.ts"
      via: "Store subscription"
---

<objective>
Implement the client-side state management for the checklist, ensuring progress is tracked locally for offline resilience and synchronized to Firestore in the background.

Purpose: Implements the "Local-first" requirement (D-02).
Output: Zustand store with persistence and background sync logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-personalized-action-layer/03-CONTEXT.md
@.planning/phases/03-personalized-action-layer/03-RESEARCH.md
@apps/frontend/src/lib/onboarding-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Checklist Zustand Store</name>
  <files>apps/frontend/src/lib/stores/checklist-store.ts</files>
  <action>
    Create `apps/frontend/src/lib/stores/checklist-store.ts`.
    Implement `useChecklistStore` using `create` and `persist` middleware from Zustand.
    - State: `items: Record<string, { completed: boolean, completedAt: string | null }>`
    - Actions: `toggleItem(id: string)`, `setItems(items: Record<...>)`.
    - Set the persistence name to `voteup-checklist`.
    Ensure the store follows the pattern in `apps/frontend/src/lib/onboarding-store.ts`.
  </action>
  <verify>
    <automated>pnpm -F frontend test apps/frontend/src/lib/stores/checklist-store.test.ts</automated>
  </verify>
  <done>Zustand store correctly persists to localStorage and allows toggling items.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Firestore Background Sync</name>
  <files>apps/frontend/src/lib/sync/checklist-sync.ts</files>
  <action>
    Create `apps/frontend/src/lib/sync/checklist-sync.ts`.
    Implement a `syncChecklistToFirestore` function that:
    - Debounces updates to avoid excessive writes.
    - Uses `setDoc` with `{ merge: true }` to save the `items` map to a `checklists` collection in Firestore under the user's UID.
    - Use `onAuthStateChanged` to ensure the UID is available before syncing.
    Hook this into `useChecklistStore` using the `subscribe` method.
  </action>
  <verify>
    <automated>pnpm -F frontend test apps/frontend/src/lib/sync/checklist-sync.test.ts</automated>
  </verify>
  <done>Checklist progress is synchronized to Firestore in the background.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Ã¢â€ â€™ Firestore | Direct client-side SDK writes to database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | Firestore State | mitigate | Implement Firestore security rules to only allow users to read/write their own document in the `checklists` collection. |
| T-03-03-02 | S | Firestore Writes | mitigate | Verify `userId` against authenticated `auth.uid` in Firestore rules. |
</threat_model>

<success_criteria>
- Checklist state persists across page refreshes.
- Items can be toggled without an active internet connection.
- Data eventually reaches Firestore when online.
</success_criteria>

<output>
After completion, create `.planning/phases/03-personalized-action-layer/03-03-SUMMARY.md`
</output>
