---
phase: 03-personalized-action-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/checklist.ts
  - apps/backend/src/services/checklist/template-provider.ts
  - apps/backend/src/routes/checklist.ts
  - apps/backend/src/app.ts
autonomous: true
requirements:
  - ACT-01
must_haves:
  truths:
    - "Backend provides hardcoded 8-stage checklist templates via API"
    - "Data structures for checklist items include status, stage, and priority"
  artifacts:
    - path: "packages/contracts/src/checklist.ts"
      provides: "Checklist type definitions"
    - path: "apps/backend/src/services/checklist/template-provider.ts"
      provides: "Static ECI stage templates"
  key_links:
    - from: "apps/backend/src/routes/checklist.ts"
      to: "apps/backend/src/services/checklist/template-provider.ts"
      via: "Method call"
---

<objective>
Define the core data structures and static templates for the 8-stage election pipeline checklists, and expose them via a backend API.

Purpose: Establishes the "Hybrid" baseline (D-01) before AI customization.
Output: Shared types, template provider service, and initial API route.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-personalized-action-layer/03-CONTEXT.md
@.planning/phases/03-personalized-action-layer/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared checklist types</name>
  <files>packages/contracts/src/checklist.ts</files>
  <action>
    Create a new file `packages/contracts/src/checklist.ts` and export the following interfaces:
    - `ChecklistItem`: { id: string, title: string, description: string, stage: number, isMandatory: boolean, category: string }
    - `ChecklistTemplate`: { stage: number, stageName: string, items: ChecklistItem[] }
    - `UserChecklist`: { userId: string, items: Record<string, { completed: boolean, completedAt?: string }> }
    Use Zod to define schemas for these types for runtime validation.
  </action>
  <verify>
    <automated>pnpm -F contracts exec tsc src/checklist.ts --noEmit</automated>
  </verify>
  <done>Types defined and exported with Zod validation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Static Template Provider</name>
  <files>apps/backend/src/services/checklist/template-provider.ts</files>
  <action>
    Create `apps/backend/src/services/checklist/template-provider.ts`. 
    Implement a class `TemplateProvider` with a method `getTemplate(stage: number): ChecklistTemplate`.
    Hardcode at least 3-5 items for each of the 8 ECI stages (Registration, Nomination, Scrutiny, Withdrawal, Campaigning, Polling, Counting, Results).
    Follow D-03 (Simple Instructions) for item descriptions.
  </action>
  <verify>
    <automated>pnpm -F backend test tests/checklist-provider.test.ts</automated>
  </verify>
  <done>Template provider returns correct items for all 8 stages.</done>
</task>

<task type="auto">
  <name>Task 3: Expose Template API</name>
  <files>apps/backend/src/routes/checklist.ts, apps/backend/src/app.ts</files>
  <action>
    1. Create `apps/backend/src/routes/checklist.ts`.
    2. Add `GET /api/checklist/templates/:stage` endpoint that calls `TemplateProvider.getTemplate`.
    3. Register the new router in `apps/backend/src/app.ts`.
    Ensure the endpoint handles invalid stages with a 400 error.
  </action>
  <verify>
    <automated>curl http://localhost:3001/api/checklist/templates/1</automated>
  </verify>
  <done>Endpoint returns the expected JSON template for a given stage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client â†’ API | External requests fetch templates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | S | API Endpoint | mitigate | Use existing Firebase Auth middleware to ensure requests are authenticated (anonymous or full). |
| T-03-01-02 | I | Template Data | accept | Template content is public civic information. |
</threat_model>

<success_criteria>
- Shared types available in monorepo.
- Backend API returns valid stage-specific checklists.
</success_criteria>

<output>
After completion, create `.planning/phases/03-personalized-action-layer/03-01-SUMMARY.md`
</output>
