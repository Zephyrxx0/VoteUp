---
phase: "04"
plan: "03"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/frontend/src/lib/stores/social-pulse-store.ts
  - apps/frontend/src/__tests__/badge-threshold.test.tsx
  - apps/frontend/src/components/social-pulse/PulseCounter.tsx
  - apps/frontend/src/components/social-pulse/MilestoneBadge.tsx
  - apps/frontend/src/app/[locale]/dashboard/page.tsx
autonomous: true
requirements:
  - SOC-01
must_haves:
  truths:
    - "User can see aggregated community milestones"
    - "User receives badges for personal and community achievements"
  artifacts:
    - path: "apps/frontend/src/lib/stores/social-pulse-store.ts"
      provides: "Zustand state for pulse counts"
      contains: "create"
    - path: "apps/frontend/src/components/social-pulse/PulseCounter.tsx"
      provides: "Counter UI"
      contains: "PulseCounter"
    - path: "apps/frontend/src/components/social-pulse/MilestoneBadge.tsx"
      provides: "Badge wrapper"
      contains: "badge"
  key_links:
    - from: "apps/frontend/src/app/[locale]/dashboard/page.tsx"
      to: "apps/frontend/src/components/social-pulse/PulseCounter.tsx"
      via: "Imports and renders component"
---

<objective>
Implement Zustand store for pulse metrics, PulseCounter component, and MilestoneBadge.

Purpose: Display the community momentum (Social Pulse) and award badges for achievements (D-02, D-03).
Output: Zustand store, UI components, and Dashboard integration.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-social-pulse-ai-comparison/04-CONTEXT.md
@.planning/phases/04-social-pulse-ai-comparison/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Social Pulse Store & Badge Threshold Logic</name>
  <files>apps/frontend/src/lib/stores/social-pulse-store.ts, apps/frontend/src/__tests__/badge-threshold.test.tsx</files>
  <behavior>
    - Test 1: evaluates community threshold based on fetched counts
  </behavior>
  <action>
    Implement `social-pulse-store.ts` using Zustand. Include state for `stageCounts` and methods to `fetchCounts(acId, stage)` from `/api/pulse`.      
    Implement logic to determine if a badge threshold is met (e.g., > 100 users).
    Write tests in `badge-threshold.test.tsx` (using vitest) to ensure threshold logic evaluates correctly based on mocked state.
  </action>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/__tests__/badge-threshold.test.tsx --passWithNoTests</automated>
  </verify>
  <done>Store handles fetching counts and determining thresholds.</done>
</task>

<task type="auto">
  <name>Task 2: Pulse Components & Dashboard Integration</name>
  <files>apps/frontend/src/components/social-pulse/PulseCounter.tsx, apps/frontend/src/components/social-pulse/MilestoneBadge.tsx, apps/frontend/src/app/[locale]/dashboard/page.tsx</files>
  <action>
    Create `PulseCounter.tsx` displaying aggregated milestone counts from the store.
    Create `MilestoneBadge.tsx` that wraps existing `apps/frontend/src/components/ui/badge.tsx` (per D-03) and displays when personal or community thresholds are met.
    Integrate both components into `dashboard/page.tsx`. Ensure they read from `social-pulse-store`.
  </action>
  <verify>
    <automated>grep -q "PulseCounter" apps/frontend/src/app/[locale]/dashboard/page.tsx</automated>
  </verify>
  <done>Dashboard displays pulse metrics and milestone badges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User→Browser | User views badge milestones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | `social-pulse-store.ts` | accept | Client-side state can be manipulated, but badges are cosmetic and do not grant elevated privileges. |
</threat_model>

<verification>
Check that the dashboard successfully renders the Pulse Counter and any earned badges.
</verification>

<success_criteria>
Social Pulse UI correctly fetches aggregated data from the backend and displays community milestones and badges.
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-pulse-ai-comparison/04-03-SUMMARY.md`
</output>