---
phase: "04"
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/backend/src/services/ai/comparison-generator.ts
  - apps/backend/src/tests/comparison.test.ts
  - apps/backend/src/routes/ai-comparison.ts
  - apps/backend/src/app.ts
autonomous: true
requirements:
  - AI-01
user_setup:
  - service: gemini
    why: "AI Comparison Generation"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio"
must_haves:
  truths:
    - "AI comparisons load securely and are formatted correctly"
    - "Users can compare their home country with India's system"
  artifacts:
    - path: "apps/backend/src/services/ai/comparison-generator.ts"
      provides: "Gemini integration"
      exports: ["generateComparison"]
    - path: "apps/backend/src/routes/ai-comparison.ts"
      provides: "API endpoint"
      contains: "router.post"
  key_links:
    - from: "apps/backend/src/routes/ai-comparison.ts"
      to: "apps/backend/src/services/ai/comparison-generator.ts"
      via: "Route handler calls generateComparison"
---

<objective>
Implement Gemini-powered comparison generator with Zod validation and expose via API.

Purpose: Provide the foundational AI capability to compare election systems while enforcing structured JSON output.
Output: AI service wrapper, Zod schemas, and `/api/compare` endpoint.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-social-pulse-ai-comparison/04-CONTEXT.md
@.planning/phases/04-social-pulse-ai-comparison/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AI Comparison Generator & Tests</name>
  <files>apps/backend/src/services/ai/comparison-generator.ts, apps/backend/src/tests/comparison.test.ts</files>
  <behavior>
    - Test 1: generateComparison produces validated JSON matching the Zod schema
  </behavior>
  <action>
    Implement `generateComparison(homeCountry: string)` in `comparison-generator.ts`.
    Use `@google/generative-ai` with `GEMINI_API_KEY`.
    Set `responseMimeType: 'application/json'` in generationConfig.
    Define a Zod schema for the expected JSON (e.g. `{ comparison: [{ category, homeCountryValue, indiaValue }] }`).
    Prompt Gemini to compare `homeCountry` and India's election systems and parse the response with Zod.
    In `comparison.test.ts`, mock `getGenerativeModel` to return valid JSON and verify parsing.
  </action>
  <verify>
    <automated>npm --prefix apps/backend run test -- src/tests/comparison.test.ts</automated>
  </verify>
  <done>Generator returns Zod-validated JSON comparison.</done>
</task>

<task type="auto">
  <name>Task 2: AI Comparison API Route</name>
  <files>apps/backend/src/routes/ai-comparison.ts, apps/backend/src/app.ts</files>
  <action>
    Create `ai-comparison.ts` Express router. Add POST `/api/compare` accepting `{ homeCountry }`.
    Validate input with Zod, call `generateComparison(homeCountry)`. Handle errors (500).
    Wire `aiComparisonRouter` into `app.ts`.
  </action>
  <verify>
    <automated>grep -q "ai-comparison" apps/backend/src/app.ts</automated>
  </verify>
  <done>API endpoint accepts country and returns structured comparison data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Untrusted client submits home country string. |
| API→Gemini | Backend sends prompt to external LLM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | `comparison-generator.ts` | mitigate | Use Zod to validate/sanitize the `homeCountry` input and strict JSON schema for Gemini output. |
| T-04-04 | Information Disclosure | `ai-comparison.ts` | mitigate | API key kept strictly server-side, frontend only receives sanitized JSON. |       
</threat_model>

<verification>
Check that the `/api/compare` endpoint successfully returns a JSON structure mapping home country aspects to India.
</verification>

<success_criteria>
Backend correctly integrates with Gemini and exposes structured JSON comparison via API.
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-pulse-ai-comparison/04-02-SUMMARY.md`
</output>