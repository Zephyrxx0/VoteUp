---
phase: "04"
plan: "04"
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - apps/frontend/src/lib/stores/ai-comparison-store.ts
  - apps/frontend/src/__tests__/ai-comparison.test.tsx
  - apps/frontend/src/components/ai-comparison/ComparisonCards.tsx
  - apps/frontend/src/app/[locale]/dashboard/page.tsx
autonomous: true
requirements:
  - AI-01
must_haves:
  truths:
    - "User views responsive comparison cards"
    - "Comparison data is cached locally to avoid refetching"
  artifacts:
    - path: "apps/frontend/src/lib/stores/ai-comparison-store.ts"
      provides: "Zustand cache"
      contains: "persist"
    - path: "apps/frontend/src/components/ai-comparison/ComparisonCards.tsx"
      provides: "Comparison UI"
      contains: "flex-col"
  key_links:
    - from: "apps/frontend/src/components/ai-comparison/ComparisonCards.tsx"
      to: "apps/frontend/src/lib/stores/ai-comparison-store.ts"
      via: "Reads cached AI output"
---

<objective>
Implement Zustand store for caching AI comparison, responsive ComparisonCards, and integrate into Dashboard.

Purpose: Display the AI-generated system comparison dynamically in a mobile-friendly way (D-01) while minimizing API calls.
Output: Zustand cached store, responsive UI cards, and finalized Dashboard.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-social-pulse-ai-comparison/04-CONTEXT.md
@.planning/phases/04-social-pulse-ai-comparison/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AI Comparison Store & UI Tests</name>
  <files>apps/frontend/src/lib/stores/ai-comparison-store.ts, apps/frontend/src/__tests__/ai-comparison.test.tsx</files>
  <behavior>
    - Test 1: Store caches comparison data locally
  </behavior>
  <action>
    Implement `ai-comparison-store.ts` using Zustand with `persist` middleware to cache Gemini results locally.
    Add method to `fetchComparison(homeCountry)` from backend `/api/compare`.
    Create a basic test scaffolding in `ai-comparison.test.tsx` (using vitest) to verify store caching behavior or mocked UI.
  </action>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/__tests__/ai-comparison.test.tsx --passWithNoTests</automated>
  </verify>
  <done>Comparison data is cached locally preventing repeated API calls.</done>
</task>

<task type="auto">
  <name>Task 2: Comparison Cards & Dashboard Integration</name>
  <files>apps/frontend/src/components/ai-comparison/ComparisonCards.tsx, apps/frontend/src/app/[locale]/dashboard/page.tsx</files>
  <action>
    Create `ComparisonCards.tsx`. Use Tailwind to ensure layout is Side-by-side on desktop (`md:flex-row`) and Stacked on mobile (`flex-col`) per D-01.
    The component should consume `ai-comparison-store` and display the parsed JSON comparison data.
    Integrate `ComparisonCards` into `dashboard/page.tsx`.
  </action>
  <verify>
    <automated>grep -q "ComparisonCards" apps/frontend/src/app/[locale]/dashboard/page.tsx</automated>
  </verify>
  <done>Dashboard renders responsive Gemini comparison cards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Browser | AI data rendered in UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Spoofing | `ai-comparison-store.ts` | mitigate | Cache comparisons using Zustand persist, reducing API calls and preventing abuse. |       
</threat_model>

<verification>
Check that comparison cards stack vertically on small screens and side-by-side on wide screens.
</verification>

<success_criteria>
AI comparison is successfully fetched, cached, and rendered cleanly on both mobile and desktop.
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-pulse-ai-comparison/04-04-SUMMARY.md`
</output>