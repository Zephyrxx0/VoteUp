---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/services/db/pulse-store.ts
  - apps/backend/src/tests/pulse.test.ts
  - apps/backend/src/routes/pulse.ts
  - apps/backend/src/app.ts
autonomous: true
requirements:
  - SOC-01
must_haves:
  truths:
    - "Community milestone progress is fast and responsive"
    - "Constituency progress updates reliably"
  artifacts:
    - path: "apps/backend/src/services/db/pulse-store.ts"
      provides: "Firestore count aggregation"
      exports: ["getStageCompletionCount"]
    - path: "apps/backend/src/routes/pulse.ts"
      provides: "REST endpoint for counts"
      contains: "router.get"
  key_links:
    - from: "apps/backend/src/routes/pulse.ts"
      to: "apps/backend/src/services/db/pulse-store.ts"
      via: "Route handler calls getStageCompletionCount"
---

<objective>
Implement Firestore `count()` aggregation for community reports and expose via API.

Purpose: Support the Social Pulse feature securely and efficiently without overloading Firestore.
Output: Firestore aggregation service and an Express API endpoint.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-social-pulse-ai-comparison/04-CONTEXT.md
@.planning/phases/04-social-pulse-ai-comparison/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Firestore Pulse Store & Tests</name>
  <files>apps/backend/src/services/db/pulse-store.ts, apps/backend/src/tests/pulse.test.ts</files>
  <behavior>
    - Test 1: getStageCompletionCount returns aggregated count for matching acId and stage
  </behavior>
  <action>
    Implement `getStageCompletionCount(acId: string, stage: number)` in `pulse-store.ts`.
    Use Firestore native `count()` aggregation (per D-02 and RESEARCH): `db.collection('reports').where('acId', '==', acId).where('reportedStage', '==', stage.toString()).count().get()`. Do NOT fetch raw documents.
    Export this function.
    In `pulse.test.ts`, write test scaffolding (using `node:test` or backend's test runner) to mock `getFirestore` and verify `.count().get()` is called correctly.
  </action>
  <verify>
    <automated>npm --prefix apps/backend run test -- src/tests/pulse.test.ts</automated>
  </verify>
  <done>Store accurately queries count using aggregation; test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Pulse API Route</name>
  <files>apps/backend/src/routes/pulse.ts, apps/backend/src/app.ts</files>
  <action>
    Create `pulse.ts` router. Add GET `/api/pulse/:acId/stages/:stage` that calls `getStageCompletionCount`.
    Return JSON `{ acId, stage, count }`. Validate parameters (e.g., stage is numeric).
    Wire `pulseRouter` into `app.ts` under `/api/pulse`.
  </action>
  <verify>
    <automated>grep -q "pulseRouter" apps/backend/src/app.ts</automated>
  </verify>
  <done>Endpoint exists and is wired up in app.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Untrusted client requests pulse data for an AC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | `pulse.ts` | mitigate | Use Firestore `.count()` instead of `.get()` to prevent massive data fetch and memory exhaustion. |
| T-04-02 | Information Disclosure | `pulse.ts` | accept | Aggregated counts are non-PII and designed for community visibility. |
</threat_model>

<verification>
Check that the endpoint returns a valid number using the aggregation function.
</verification>

<success_criteria>
Backend accurately aggregates constituency stage counts using Firestore `count()` and exposes it via `/api/pulse`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-pulse-ai-comparison/04-01-SUMMARY.md`
</output>