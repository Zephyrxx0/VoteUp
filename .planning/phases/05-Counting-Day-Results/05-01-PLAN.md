---
phase: 05-Counting-Day-Results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/backend/src/services/scraper/results-engine.ts, apps/backend/src/services/db/results-cache.ts, apps/backend/src/services/db/constituency-store.ts, apps/backend/src/routes/status.ts, apps/backend/src/app.ts]
autonomous: true
requirements: [RES-01, RES-02]

must_haves:
  truths:
    - "Backend resolves Stage 8 (Counting) automatically if the date is May 4, 2026."
    - "Live scraper extracts candidate results from ECI portal."
    - "Scraped results are cached for 5 minutes."
    - "New API endpoint returns both current stage and latest results."
  artifacts:
    - path: "apps/backend/src/services/scraper/results-engine.ts"
      provides: "Playwright-based live results scraper"
    - path: "apps/backend/src/services/db/results-cache.ts"
      provides: "5-minute TTL in-memory cache"
    - path: "apps/backend/src/routes/status.ts"
      provides: "GET /api/constituency/:id/status endpoint"
  key_links:
    - from: "apps/backend/src/routes/status.ts"
      to: "apps/backend/src/services/scraper/results-engine.ts"
      via: "scrapers results if cache missed"
---

<objective>
Implement the backend infrastructure for live election results and time-aware stage resolution.

Purpose: To provide a reliable, cached source of live counting data for the May 4 demo.
Output: Scraper engine, caching layer, updated stage resolver, and a new unified status API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-Counting-Day-Results/05-CONTEXT.md
@.planning/phases/05-Counting-Day-Results/05-RESEARCH.md
@apps/backend/src/services/scraper/eci-portal.ts
@apps/backend/src/services/db/constituency-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Results Scraper & Cache</name>
  <files>apps/backend/src/services/scraper/results-engine.ts, apps/backend/src/services/db/results-cache.ts</files>
  <action>
    - Create `results-cache.ts` providing a simple singleton `ResultsCache` with a 5-minute TTL (per D-05-01).
    - Create `results-engine.ts` using Playwright to scrape `results.eci.gov.in`. 
    - Use the "Header-Relative Extraction" pattern from RESEARCH.md to avoid brittle nth-child selectors.
    - Extract: Candidate Name, Party, Votes, and Status (Leading/Won).
    - Implementation must handle Akamai 403s by setting realistic User-Agents (D-05-01).
  </action>
  <verify>
    <automated>node --test apps/backend/src/tests/scraper.test.ts</automated>
  </verify>
  <done>Scraper successfully extracts results from a mock ECI HTML page; Cache expires after 5 mins.</done>
</task>

<task type="auto">
  <name>Task 2: Update Stage Resolver & Create Status API</name>
  <files>apps/backend/src/services/db/constituency-store.ts, apps/backend/src/routes/status.ts, apps/backend/src/app.ts</files>
  <action>
    - Update `resolveCurrentStage` in `constituency-store.ts` to implement the logic from D-05-02.
    - Logic: If `now >= counting_date`, return 'Counting'. Default counting_date to May 4, 2026.
    - Create `apps/backend/src/routes/status.ts` with `GET /api/constituency/:id/status`.
    - The endpoint should return `{ stage: Stage, results: CandidateResult[] }`.
    - Register the new router in `apps/backend/src/app.ts` under `/api/constituency`.
  </action>
  <verify>
    <automated>node --test apps/backend/src/tests/stage.test.ts</automated>
  </verify>
  <done>API returns "Counting" and live/cached results for a constituency on May 4, 2026.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → ECI Portal | Scraper interacts with external untrusted HTML |
| Client → API | User requests constituency status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | S | results-engine.ts | mitigate | Validate constituency ID before passing to ECI URL; No user-controlled URLs in page.goto. |
| T-05-02 | D | status.ts | mitigate | 5-minute server-side caching to prevent ECI rate limiting and backend resource exhaustion. |
</threat_model>

<verification>
- Run `node --test apps/backend/src/tests/scraper.test.ts` to verify extraction.
- Run `node --test apps/backend/src/tests/stage.test.ts` to verify time-aware transitions.
- Curl `http://localhost:3000/api/constituency/S2477/status` and verify JSON structure.
</verification>

<success_criteria>
- Scraper extracts 100% of candidates from valid ECI table.
- Stage resolves to 'Counting' when system clock is set to May 4, 2026.
- API response time is < 100ms for cached hits.
</success_criteria>

<output>
After completion, create `.planning/phases/05-Counting-Day-Results/05-01-SUMMARY.md`
</output>
