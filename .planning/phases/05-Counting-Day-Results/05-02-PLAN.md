---
phase: 05-Counting-Day-Results
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [apps/frontend/src/components/ui/result-card.tsx, apps/frontend/src/app/[locale]/dashboard/page.tsx, apps/frontend/src/lib/stores/social-pulse-store.ts]
autonomous: true
requirements: [RES-03, RES-04]

must_haves:
  truths:
    - "Dashboard shows a blinking 'LIVE' indicator during Stage 8 (Counting)."
    - "Live results list is displayed on the dashboard when in Stage 8."
    - "Winning candidate is highlighted with a distinct 'Winner' badge."
  artifacts:
    - path: "apps/frontend/src/components/ui/result-card.tsx"
      provides: "Mobile-first candidate result card"
    - path: "apps/frontend/src/app/[locale]/dashboard/page.tsx"
      provides: "Live-updated dashboard with status polling"
  key_links:
    - from: "apps/frontend/src/app/[locale]/dashboard/page.tsx"
      to: "/api/constituency/:id/status"
      via: "fetch/swr every 60s"
---

<objective>
Implement the frontend UI for live election results and dashboard stage transition.

Purpose: To visualize the counting process and live candidate standings for citizens.
Output: Result Card components and a live-updating Dashboard integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-Counting-Day-Results/05-CONTEXT.md
@.planning/phases/05-Counting-Day-Results/05-RESEARCH.md
@apps/frontend/src/app/[locale]/dashboard/page.tsx
@apps/frontend/src/components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Result Cards & List</name>
  <files>apps/frontend/src/components/ui/result-card.tsx</files>
  <action>
    - Create a `ResultCard` component using `ui/card.tsx` as base.
    - Fields: Candidate Name, Party, Votes (formatted with commas), Status (Leading/Won).
    - Styling: Use a vertical list for mobile-first readability (D-05-03).
    - Highlight the "Winner" (Status: Won) with a special badge (e.g., `Badge` from shadcn with primary color).
  </action>
  <verify>
    <automated>pnpm test apps/frontend/src/__tests__/result-card.test.tsx</automated>
  </verify>
  <done>ResultCard renders correctly with mock data; Winner badge appears for status 'Won'.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Live Integration</name>
  <files>apps/frontend/src/app/[locale]/dashboard/page.tsx, apps/frontend/src/lib/stores/social-pulse-store.ts</files>
  <action>
    - Update the Dashboard to fetch from `GET /api/constituency/:id/status` (created in 05-01).
    - Implement a "Live" blinking indicator (D-05-02 / Specifics) using CSS animation and Lucide `Circle` icon.
    - Show the indicator ONLY when `stage === 'Counting'`.
    - If `stage === 'Counting'`, render the list of `ResultCard` components below the stage progress.
    - Implement polling or SWR logic to refresh results every 60 seconds.
  </action>
  <verify>
    <automated>pnpm test apps/frontend/src/__tests__/dashboard-live.test.tsx</automated>
  </verify>
  <done>Dashboard shows live results and blinking indicator when stage is 'Counting'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | UI fetches live status and results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | I | dashboard/page.tsx | mitigate | Sanitize results data from API before rendering to prevent XSS. |
</threat_model>

<verification>
- Start backend and frontend.
- Navigate to `/en/dashboard`.
- Use DevTools to mock API response with `stage: 'Counting'` and check UI.
- Verify "Live" indicator is blinking.
- Verify Winner badge is correctly applied to the candidate with `status: 'Won'`.
</verification>

<success_criteria>
- Dashboard automatically switches to "Counting" view when API returns Stage 8.
- "Live" indicator blinks at ~1Hz.
- Candidate results are sorted by votes descending.
</success_criteria>

<output>
After completion, create `.planning/phases/05-Counting-Day-Results/05-02-SUMMARY.md`
</output>
