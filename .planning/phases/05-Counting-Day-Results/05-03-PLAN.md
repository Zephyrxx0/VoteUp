---
phase: 05-Counting-Day-Results
plan: 03
type: execute
wave: 3
depends_on: [05-01]
files_modified: [apps/backend/src/services/db/pulse-store.ts, apps/backend/src/services/db/audit-log.ts, apps/frontend/src/app/[locale]/dashboard/page.tsx, apps/backend/src/services/db/constituency-store.ts]
autonomous: true
requirements: [RES-05]

must_haves:
  truths:
    - "Pulse and Audit logs successfully sync to Firestore if credentials exist."
    - "App gracefully falls back to mock data if Firebase credentials are missing."
    - "Users can 'Share Journey' once the result is declared."
  artifacts:
    - path: "apps/backend/src/services/db/pulse-store.ts"
      provides: "Firestore-backed pulse persistence"
    - path: "apps/backend/src/services/db/audit-log.ts"
      provides: "Firestore-backed audit logging"
  key_links:
    - from: "apps/backend/src/services/db/pulse-store.ts"
      to: "Firestore"
      via: "firebase-admin SDK"
---

<objective>
Finalize the Firestore sync logic and add demo-specific polish for a production-ready feel.

Purpose: To ensure data persistence across sessions and provide a high-quality finish for the demo.
Output: Production-ready Firebase integration and "Share Journey" social feature.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-Counting-Day-Results/05-CONTEXT.md
@.planning/phases/05-Counting-Day-Results/05-RESEARCH.md
@apps/backend/src/services/db/pulse-store.ts
@apps/backend/src/services/db/constituency-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Firestore Sync Logic</name>
  <files>apps/backend/src/services/db/pulse-store.ts, apps/backend/src/services/db/audit-log.ts</files>
  <action>
    - Implement the Firestore persistence logic in `pulse-store.ts` (currently likely using mocks).
    - Implement `logAction` in `audit-log.ts` to write to the `audit_logs` Firestore collection.
    - Ensure all Firestore operations check `firestoreDb !== null` and log a standard warning if null (per D-05-04).
    - Handle standard Firestore write errors with graceful catch blocks.
  </action>
  <verify>
    <automated>node --test apps/backend/src/tests/firebase-sync.test.ts</automated>
  </verify>
  <done>Pulse data and audit logs are written to Firestore when credentials are provided; system doesn't crash without them.</done>
</task>

<task type="auto">
  <name>Task 2: Demo Polish & Share Journey</name>
  <files>apps/frontend/src/app/[locale]/dashboard/page.tsx, apps/backend/src/services/db/constituency-store.ts</files>
  <action>
    - Add a "Share Journey" button to the Dashboard (D-05-04 / Specifics).
    - Visibility: Only show when `stage === 'Result Declared'`.
    - Functionality: Simple Web Share API call (or copy to clipboard) with a pre-filled message: "I followed the election journey for [Constituency] on VoteUp! Result: [Winner] won!"
    - Ensure demo constituencies (e.g., 'S2477') have May 4, 2026, as the default counting date in the fallback logic.
  </action>
  <verify>
    <automated>pnpm test apps/frontend/src/__tests__/demo-polish.test.tsx</automated>
  </verify>
  <done>Share button appears only at final stage; Demo defaults are correctly set for May 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → Firestore | Persistence of system and user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | I | pulse-store.ts | mitigate | Use Firestore Security Rules (when deployed) and Custom Claims via Firebase Admin. |
</threat_model>

<verification>
- Provide valid Firebase env vars (or mock them in tests).
- Run `node --test apps/backend/src/tests/firebase-sync.test.ts`.
- Manually check Dashboard state for 'Result Declared' stage and verify Share button appears.
</verification>

<success_criteria>
- Zero crashes when Firebase credentials are missing.
- Audit logs contain timestamp, user (if auth), and action description.
- Share button uses native Web Share API where supported.
</success_criteria>

<output>
After completion, create `.planning/phases/05-Counting-Day-Results/05-03-SUMMARY.md`
</output>
