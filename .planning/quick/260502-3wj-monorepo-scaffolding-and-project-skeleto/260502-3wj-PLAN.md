---
phase: quick
plan: 260502-3wj
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - turbo.json
  - .gitignore
  - apps/web/package.json
  - apps/api/package.json
  - apps/ai/package.json
  - apps/ai/requirements.txt
  - packages/ui/package.json
  - packages/types/package.json
  - packages/config/package.json
autonomous: true
requirements: [SYS-01]
must_haves:
  truths:
    - "Monorepo structure is initialized with functional workspaces for web, api, and ai."
    - "Root package.json correctly lists apps/* and packages/* in workspaces."
    - "Turborepo is configured to manage build and dev pipelines."
  artifacts:
    - path: "package.json"
      provides: "Root workspace management"
    - path: "turbo.json"
      provides: "Pipeline orchestration"
    - path: "apps/web/package.json"
      provides: "Next.js frontend placeholder"
    - path: "apps/api/package.json"
      provides: "Node.js API placeholder"
    - path: "apps/ai/package.json"
      provides: "Python AI placeholder scripts"
  key_links:
    - from: "package.json"
      to: "apps/web"
      via: "workspaces"
    - from: "turbo.json"
      to: "apps/web"
      via: "pipeline configuration"
---

<objective>
Initialize the VoteUp monorepo using Turborepo to provide a clean separation between the Next.js frontend, Node.js API, and Python AI services, along with shared configurations and types.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Initialize monorepo root and workspace configuration</name>
  <files>package.json, turbo.json, .gitignore</files>
  <action>
    Create the root package.json with workspace support for 'apps/*' and 'packages/*'. 
    Configure turbo.json with basic pipelines: 'build' (depends on ^build), 'lint', and 'dev' (persistent).
    Update .gitignore to include node_modules, .turbo, dist, .next, and __pycache__.
    Reference STACK.md for npm-based workspace conventions.
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); if (!pkg.workspaces.includes('apps/*')) process.exit(1)"</automated>
  </verify>
  <done>Root configuration files created and workspaces defined.</done>
</task>

<task type="auto">
  <name>Scaffold application placeholders</name>
  <files>apps/web/package.json, apps/api/package.json, apps/ai/package.json, apps/ai/requirements.txt</files>
  <action>
    Create placeholder package.json files for:
    - apps/web: Named '@voteup/web', includes next, react, react-dom.
    - apps/api: Named '@voteup/api', includes express, typescript, zod.
    - apps/ai: Named '@voteup/ai', includes scripts for python execution.
    Create apps/ai/requirements.txt with llama-index and fast-api placeholders.
    Ensure all apps have a 'build' script (even if it's just 'echo 0').
  </action>
  <verify>
    <automated>Test-Path apps/web/package.json, apps/api/package.json, apps/ai/package.json</automated>
  </verify>
  <done>Application directories scaffolded with basic package metadata.</done>
</task>

<task type="auto">
  <name>Scaffold shared package placeholders</name>
  <files>packages/ui/package.json, packages/types/package.json, packages/config/package.json</files>
  <action>
    Create placeholder package.json files for:
    - packages/config: Named '@voteup/config', shared eslint/tsconfig base.
    - packages/types: Named '@voteup/types', shared TS interfaces.
    - packages/ui: Named '@voteup/ui', shared React components.
    These packages should have version '0.0.0' and main/types fields pointing to dist or src.
  </action>
  <verify>
    <automated>Test-Path packages/ui/package.json, packages/types/package.json, packages/config/package.json</automated>
  </verify>
  <done>Shared packages scaffolded to support cross-app code sharing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Monorepo Root | Untrusted dependencies from npm registry |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QUICK-01 | Tampering | package.json | mitigate | Use package-lock.json (to be generated) to ensure deterministic builds |
| T-QUICK-02 | Information Disclosure | .gitignore | mitigate | Ensure sensitive files like .env and secrets are explicitly ignored |
</threat_model>

<success_criteria>
- Turborepo root is initialized.
- Workspaces for web, api, ai, and packages are defined.
- 'npm install' can be executed from the root (infrastructure ready for next phase).
</success_criteria>

<output>
After completion, create .planning/quick/260502-3wj-monorepo-scaffolding-and-project-skeleto/260502-3wj-SUMMARY.md
</output>
